const nome = 'Ricardo'; //string
const num1 = 12; // number
let nomeAluno; //undefined -> Não aponta para nada na memória (normalmente retorno de erros)
const sobrenomeAluno = null; // Nulo -> Não aponta para nada na memória (Deliberadamente quero que o valor seja nulo, caso não tenha nada selecionado, por ex)
const boolean = true; // boolean -> true or false (0,1) - Lógico